# Models

This folder is reserved for trained machine learning models.

The proposed framework employs:

- Random Forest
- XGBoost
- Hybrid Ensemble Learning

Models are trained using both the original dataset and SMOTE-balanced data to evaluate the impact of class imbalance handling.

No pre-trained models are included in this repository.
